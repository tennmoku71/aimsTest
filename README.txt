hogehoge

fugafuga

#もし変更を更新するときは
git add .
git commit -m "my commit!"
git push origin master

#もし変更をダウンロードするときは
git pull origin master

#もし新しいブランチを作るときは
git checkout -b ブランチ名

#masterブランチに戻るときは
git checkout master

#今のブランチを確認するときは
git branch

#今の状態を確認するt気は
git status

#コミットログの確認
git log
